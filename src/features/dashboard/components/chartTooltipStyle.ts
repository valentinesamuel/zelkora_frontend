// Shared `contentStyle` for every Recharts `<Tooltip>` on the dashboard.
export const CHART_TOOLTIP_STYLE = {
  background: 'var(--popover)',
  border: '1px solid var(--border)',
  borderRadius: 'var(--radius-md)',
  color: 'var(--popover-foreground)',
  fontSize: 12,
} as const;
